[pytest]
asyncio_mode = auto
; repo root (one level up) so tests can `import customer_intelligence...` / `import security...`
; as ordinary sibling packages, same as main.py does at runtime.
pythonpath = ..
